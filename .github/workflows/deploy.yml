# Build Vite app and publish ./dist to gh-pages for GitHub Pages hosting
name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18
          # cache removed because there is no lockfile in the repo

      - name: (Optional) Create .env.local from secret
        # If your build needs GEMINI_API_KEY at build-time, set it in repo Settings → Secrets as GEMINI_API_KEY
        run: |
          if [ -n "${{ secrets.GEMINI_API_KEY }}" ]; then
            echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" > .env.local
          fi

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: ./dist
          publish_user_name: github-actions[bot]
          publish_user_email: 41898282+github-actions[bot]@users.noreply.github.com
